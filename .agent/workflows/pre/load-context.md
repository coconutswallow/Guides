---
description: Concise context loader
---

# Pre-Task Checklist

Read these files to synchronize with the current state:

## 1. Primary Context
- `view_file` on `.Context/changelog.md`.
- `view_file` on `.Context/database-schema.md`.
- `list_dir` on `.Context/current-tasks/` and read all files inside.

## 2. Secondary Context (If Relevant)
- `view_file` on `.Context/directory.md` (if structural).
- `view_file` on `.Context/JSONB_REFERENCE.md` (if work with models/visuals).

## 3. Limits
- Keep load under 5,000 tokens.
- Skip `/archive` and `/parked` folders.